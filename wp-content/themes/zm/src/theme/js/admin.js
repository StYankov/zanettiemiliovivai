// should be loaded by admin_enqueue_scripts hook(/library/enqueue-scripts.php).
// This file is used for gutenberg admin related js functionalities (e.g. hooks, filters and whatever you need in gutenberg admin context)

jQuery(document).ready(function () {
	// wp.hooks.addFilter('filter_name', 'namespace',  function(){
	// 	return ...;
	// });
});
